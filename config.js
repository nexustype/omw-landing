window.OMW_CONFIG = {
  // Your form endpoint. For Vercel serverless, use "/api/submit".
  riderFormEndpoint: "/api/submit",
  driverFormEndpoint: "/api/submit",

  // Fallback email for display only (no auto mailto).
  fallbackEmail: "hello@rideomw.com"
};
