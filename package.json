{
  "name": "omw-landing",
  "private": true,
  "scripts": {
    "build:presentation": "esbuild presentation/main.jsx --bundle --format=esm --jsx=automatic --minify --define:process.env.NODE_ENV=\\\"production\\\" --loader:.png=file --loader:.svg=file --asset-names=assets/[name]-[hash] --outfile=presentation/app.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.14"
  },
  "devDependencies": {
    "esbuild": "^0.25.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
